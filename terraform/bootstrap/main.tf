# This independent stack intentionally contains no resources yet.
#
# State storage, GitHub OIDC providers, and CI roles remain scaffold-only until
# their immutable GitHub owner/repository IDs and the state bootstrap sequence
# are supplied. The S3 backend is deliberately configured at init time.
